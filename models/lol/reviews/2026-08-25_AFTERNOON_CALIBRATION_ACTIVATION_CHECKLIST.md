# Afternoon Calibration Activation Checklist — 2026-08-25

Before declaring the calibration canonical:

- amend `models/lol/CURRENT_MODEL.md`;
- add new procedures to mandatory load order;
- amend PRE_TAKE validator to include dog-cushion class and PML checks;
- ensure future session locks inherit the new authority commit;
- verify files from default branch after writes.

Do not claim canonical activation until these steps are complete.
